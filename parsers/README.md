# `parsers/` — Parsers

| Module | Purpose |
|--------|---------|
| `ast_parser.py` | AST-based symbol extraction for Python (`ast` module) |
| `edit_parser.py` | Parses and validates SEARCH/REPLACE and edit-line formats |